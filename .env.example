# OpenAI API Key (optional - users can add their own via Settings)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Blockscout Client Mode (optional)
# Set to 'true' to use HTTP client instead of MCP (no Docker required)
# Recommended if you don't have Docker installed
BLOCKSCOUT_USE_HTTP=true

# Blockscout MCP Server URL (optional, only used with MCP client)
BLOCKSCOUT_MCP_URL=http://localhost:3001
